id = "ledgermem"
name = "LedgerMem"
description = "Persistent memory for AI coding workflows in Zed."
version = "0.1.0"
schema_version = 1
authors = ["Proofly <founders@proofly.dev>"]
repository = "https://github.com/ledgermem/ledgermem-zed"

[slash_commands.lm-search]
description = "Search LedgerMem"
requires_argument = true

[slash_commands.lm-add]
description = "Add the current selection (or argument) to LedgerMem"
requires_argument = false
